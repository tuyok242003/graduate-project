import styled from 'styled-components';

// Styled component cho sale badge
export const SaleBadge = styled.span`
  position: absolute;
  top: 0;
  right: 0;
  background-color: red;
  color: white;
  padding: 5px;
  font-size: 15px;
  font-weight: bold;
`;

// Styled component cho product title
export const ProductTitle = styled.div`
  .product-title {
    font-family: display;
  }
`;
