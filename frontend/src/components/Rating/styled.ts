import styled from 'styled-components';
export const RatingStyled = styled.div`
  .rating span {
    margin: 0.1rem;
  }

  .rating svg {
    color: #f8e825;
  }

  .rating-text {
    font-size: 0.8rem;
    font-weight: 600;
    padding-left: 0.5rem;
  }
`;
