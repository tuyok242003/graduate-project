import styled from "styled-components"
export const HeaderHome = styled.div`
  .badge {
    margin-left: 5px;
  }
  .home {
    padding-right: 60px;
  }
  .post {
    padding-right: 60px;
  }
  .contact {
    padding-right: 60px;
  }
`
